[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx next build"]

[start]
cmd = "npx prisma db push --skip-generate --accept-data-loss; node .next/standalone/server.js"
